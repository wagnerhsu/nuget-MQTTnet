version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=MqttExamples_Identity
      - MqttExamples_DB=MqttExamples_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
